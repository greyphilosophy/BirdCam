--extra-index-url https://download.pytorch.org/whl/cu128

# Production Windows build for the RTX 4090. The explicit local-version tags
# prevent pip from silently substituting CPU-only PyTorch wheels.
torch==2.11.0+cu128
torchvision==0.26.0+cu128

-r requirements-common.txt
